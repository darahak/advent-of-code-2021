export default {
  /** @argument input {string} */
  first(input) {
    const lines = input.trim().split('\n');
  },

  /** @argument input {string} */
  second(input) {
    const lines = input.trim().split('\n');
  },
};
